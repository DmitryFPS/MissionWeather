# Production: лимиты памяти
services:
  api:
    deploy:
      resources:
        limits:
          memory: 768M

  web:
    deploy:
      resources:
        limits:
          memory: 512M

  gateway:
    deploy:
      resources:
        limits:
          memory: 128M
